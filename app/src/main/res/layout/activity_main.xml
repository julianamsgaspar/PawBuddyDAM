<?xml version="1.0" encoding="utf-8"?>

<!--
Layout principal com Drawer (menu lateral)
-->
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/drawerLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!--
    Conteúdo principal da aplicação
    -->
    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!--
        AppBar com a toolbar no topo
        -->
        <com.google.android.material.appbar.AppBarLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <!--
            Toolbar principal da aplicação
            -->
            <com.google.android.material.appbar.MaterialToolbar
                android:id="@+id/toolbar"
                android:layout_width="match_parent"
                android:layout_height="?attr/actionBarSize"
                app:title="@string/app_name"
                app:logo="@drawable/ic_pets"
                app:logoDescription="@string/app_name"
                app:menu="@menu/menu_top_appbar"
                android:tint="?attr/colorOnSurfaceVariant"/>
        </com.google.android.material.appbar.AppBarLayout>

        <!--
        NavHostFragment onde os fragments são carregados
        -->
        <androidx.fragment.app.FragmentContainerView
            android:id="@+id/navHostFragment"
            android:name="androidx.navigation.fragment.NavHostFragment"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:paddingBottom="56dp"
            android:clipToPadding="false"
            app:defaultNavHost="true"
            app:layout_behavior="@string/appbar_scrolling_view_behavior"
            app:navGraph="@navigation/nav_graph"/>

    </androidx.coordinatorlayout.widget.CoordinatorLayout>

    <!--
    Drawer lateral (menu de administração)
    -->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/adminDrawer"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        app:headerLayout="@layout/drawer_header"
        app:menu="@menu/menu_admin_drawer"/>

</androidx.drawerlayout.widget.DrawerLayout>
